# RuoYi-Vue3 分页处理详解

## 一、分页组件概述

RuoYi-Vue3 封装了统一的分页组件 `Pagination`，基于 Element Plus 的 `el-pagination` 组件进行二次封装，提供了更便捷的使用方式。

---

## 二、分页相关文件

| 文件路径 | 作用 |
|----------|------|
| `src/components/Pagination/index.vue` | 分页组件 |
| `src/utils/scroll-to.js` | 滚动工具函数 |

---

## 三、分页组件源码解析

**文件位置**：`src/components/Pagination/index.vue`

### 3.1 组件模板

```vue
<template>
  <div :class="{ 'hidden': hidden }" class="pagination-container">
    <el-pagination
      :background="background"
      v-model:current-page="currentPage"
      v-model:page-size="pageSize"
      :layout="layout"
      :page-sizes="pageSizes"
      :pager-count="pagerCount"
      :total="total"
      @size-change="handleSizeChange"
      @current-change="handleCurrentChange"
    />
  </div>
</template>
```

### 3.2 组件属性

```javascript
const props = defineProps({
  // 总条数（必填）
  total: {
    required: true,
    type: Number
  },
  
  // 当前页码
  page: {
    type: Number,
    default: 1
  },
  
  // 每页条数
  limit: {
    type: Number,
    default: 20
  },
  
  // 每页条数选择器选项
  pageSizes: {
    type: Array,
    default() {
      return [10, 20, 30, 50]
    }
  },
  
  // 页码按钮数量（移动端默认5，桌面端默认7）
  pagerCount: {
    type: Number,
    default: document.body.clientWidth < 992 ? 5 : 7
  },
  
  // 分页布局
  layout: {
    type: String,
    default: 'total, sizes, prev, pager, next, jumper'
  },
  
  // 是否显示背景色
  background: {
    type: Boolean,
    default: true
  },
  
  // 是否自动滚动到顶部
  autoScroll: {
    type: Boolean,
    default: true
  },
  
  // 是否隐藏
  hidden: {
    type: Boolean,
    default: false
  }
})
```

### 3.3 双向绑定实现

```javascript
const emit = defineEmits()

// 当前页码（双向绑定）
const currentPage = computed({
  get() {
    return props.page
  },
  set(val) {
    emit('update:page', val)
  }
})

// 每页条数（双向绑定）
const pageSize = computed({
  get() {
    return props.limit
  },
  set(val) {
    emit('update:limit', val)
  }
})
```

### 3.4 事件处理

```javascript
// 每页条数改变
function handleSizeChange(val) {
  // 如果当前页 * 新每页条数 > 总条数，则重置为第一页
  if (currentPage.value * val > props.total) {
    currentPage.value = 1
  }
  
  // 触发 pagination 事件
  emit('pagination', { page: currentPage.value, limit: val })
  
  // 自动滚动到顶部
  if (props.autoScroll) {
    scrollTo(0, 800)
  }
}

// 当前页改变
function handleCurrentChange(val) {
  emit('pagination', { page: val, limit: pageSize.value })
  
  if (props.autoScroll) {
    scrollTo(0, 800)
  }
}
```

---

## 四、分页组件使用方式

### 4.1 基本用法

```vue
<template>
  <!-- 表格 -->
  <el-table :data="userList">
    <!-- 表格列 -->
  </el-table>
  
  <!-- 分页组件 -->
  <pagination
    v-show="total > 0"
    :total="total"
    v-model:page="queryParams.pageNum"
    v-model:limit="queryParams.pageSize"
    @pagination="getList"
  />
</template>

<script setup>
import { ref, reactive } from 'vue'
import { listUser } from '@/api/system/user'

// 用户列表数据
const userList = ref([])
const total = ref(0)

// 查询参数
const queryParams = reactive({
  pageNum: 1,    // 当前页码
  pageSize: 10   // 每页条数
})

// 获取用户列表
function getList() {
  listUser(queryParams).then(res => {
    userList.value = res.rows
    total.value = res.total
  })
}

// 初始化加载数据
getList()
</script>
```

### 4.2 完整示例（带搜索条件）

```vue
<template>
  <div class="app-container">
    <!-- 搜索表单 -->
    <el-form :model="queryParams" ref="queryRef" :inline="true">
      <el-form-item label="用户名称" prop="userName">
        <el-input 
          v-model="queryParams.userName" 
          placeholder="请输入用户名称" 
          clearable
          @keyup.enter="handleQuery"
        />
      </el-form-item>
      <el-form-item label="状态" prop="status">
        <el-select v-model="queryParams.status" placeholder="用户状态" clearable>
          <el-option label="正常" value="0" />
          <el-option label="停用" value="1" />
        </el-select>
      </el-form-item>
      <el-form-item>
        <el-button type="primary" icon="Search" @click="handleQuery">搜索</el-button>
        <el-button icon="Refresh" @click="resetQuery">重置</el-button>
      </el-form-item>
    </el-form>
    
    <!-- 数据表格 -->
    <el-table v-loading="loading" :data="userList">
      <el-table-column label="用户编号" prop="userId" />
      <el-table-column label="用户名称" prop="userName" />
      <el-table-column label="状态" prop="status" />
      <el-table-column label="创建时间" prop="createTime" />
    </el-table>
    
    <!-- 分页组件 -->
    <pagination
      v-show="total > 0"
      :total="total"
      v-model:page="queryParams.pageNum"
      v-model:limit="queryParams.pageSize"
      @pagination="getList"
    />
  </div>
</template>

<script setup name="User">
import { ref, reactive } from 'vue'
import { listUser } from '@/api/system/user'

const { proxy } = getCurrentInstance()

// 加载状态
const loading = ref(false)
// 用户列表
const userList = ref([])
// 总条数
const total = ref(0)

// 查询参数
const queryParams = reactive({
  pageNum: 1,
  pageSize: 10,
  userName: undefined,
  status: undefined
})

/** 搜索按钮操作 */
function handleQuery() {
  queryParams.pageNum = 1  // 搜索时重置页码
  getList()
}

/** 重置按钮操作 */
function resetQuery() {
  proxy.resetForm('queryRef')
  handleQuery()
}

/** 获取用户列表 */
function getList() {
  loading.value = true
  listUser(queryParams).then(res => {
    userList.value = res.rows
    total.value = res.total
    loading.value = false
  })
}

// 初始化
getList()
</script>
```

### 4.3 自定义配置

```vue
<!-- 自定义每页条数选项 -->
<pagination
  :total="total"
  v-model:page="queryParams.pageNum"
  v-model:limit="queryParams.pageSize"
  :page-sizes="[20, 50, 100, 200]"
  @pagination="getList"
/>

<!-- 自定义布局 -->
<pagination
  :total="total"
  v-model:page="queryParams.pageNum"
  v-model:limit="queryParams.pageSize"
  layout="total, prev, pager, next"
  @pagination="getList"
/>

<!-- 禁用自动滚动 -->
<pagination
  :total="total"
  v-model:page="queryParams.pageNum"
  v-model:limit="queryParams.pageSize"
  :auto-scroll="false"
  @pagination="getList"
/>
```

---

## 五、分页参数传递规范

### 5.1 前端参数

```javascript
const queryParams = {
  pageNum: 1,    // 当前页码（从1开始）
  pageSize: 10,  // 每页条数
  
  // 其他查询条件
  userName: 'admin',
  status: '0'
}
```

### 5.2 后端接收参数

```java
// Spring Boot 接收分页参数
@GetMapping("/list")
public TableDataInfo list(SysUser user) {
    startPage();  // PageHelper 设置分页
    List<SysUser> list = userService.selectUserList(user);
    return getDataTable(list);
}
```

### 5.3 后端返回格式

```json
{
  "code": 200,
  "msg": "查询成功",
  "rows": [
    { "userId": 1, "userName": "admin" },
    { "userId": 2, "userName": "test" }
  ],
  "total": 100
}
```

---

## 六、分页工作流程

```
┌─────────────────────────────────────────────────────────────────┐
│                    1. 用户点击分页按钮                           │
└─────────────────────────┬───────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    2. 触发 pagination 事件                       │
│              传递 { page, limit } 参数                           │
└─────────────────────────┬───────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    3. 更新 queryParams                          │
│              pageNum 和 pageSize 自动更新                        │
└─────────────────────────┬───────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    4. 调用 getList 方法                         │
│              发送请求到后端                                       │
└─────────────────────────┬───────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    5. 后端处理分页                               │
│              使用 PageHelper 分页插件                            │
└─────────────────────────┬───────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    6. 返回分页数据                               │
│              { rows: [], total: 100 }                            │
└─────────────────────────┬───────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    7. 更新表格数据                               │
│              userList 和 total 更新                              │
└─────────────────────────┬───────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    8. 滚动到页面顶部                             │
│              （可选，autoScroll 控制）                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 七、分页常见场景处理

### 7.1 搜索时重置页码

```javascript
function handleQuery() {
  queryParams.pageNum = 1  // 搜索时重置为第一页
  getList()
}
```

### 7.2 删除最后一条数据

```javascript
function handleDelete(row) {
  proxy.$modal.confirm('确定删除？').then(() => {
    delUser(row.userId).then(() => {
      // 如果当前页只有一条数据且不是第一页，则跳转到上一页
      if (userList.value.length === 1 && queryParams.pageNum > 1) {
        queryParams.pageNum--
      }
      getList()
      proxy.$modal.msgSuccess('删除成功')
    })
  })
}
```

### 7.3 日期范围查询

```vue
<template>
  <el-form-item label="创建时间">
    <el-date-picker
      v-model="dateRange"
      type="daterange"
      value-format="YYYY-MM-DD"
      range-separator="-"
      start-placeholder="开始日期"
      end-placeholder="结束日期"
    />
  </el-form-item>
</template>

<script setup>
import { addDateRange } from '@/utils/ruoyi'

const dateRange = ref([])

function getList() {
  // 添加日期范围参数
  const params = addDateRange(queryParams, dateRange.value)
  listUser(params).then(res => {
    userList.value = res.rows
    total.value = res.total
  })
}
</script>
```

### 7.4 导出时传递分页参数

```javascript
function handleExport() {
  proxy.$modal.confirm('确定导出数据？').then(() => {
    proxy.download('/system/user/export', {
      ...queryParams,
      pageNum: undefined,   // 导出时不需要分页
      pageSize: undefined
    }, 'user.xlsx')
  })
}
```

---

## 八、分页组件 Props 完整列表

| 属性名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `total` | Number | - | 总条数（必填） |
| `page` | Number | 1 | 当前页码 |
| `limit` | Number | 20 | 每页条数 |
| `pageSizes` | Array | [10, 20, 30, 50] | 每页条数选项 |
| `pagerCount` | Number | 7 | 页码按钮数量 |
| `layout` | String | 'total, sizes, prev, pager, next, jumper' | 分页布局 |
| `background` | Boolean | true | 是否显示背景色 |
| `autoScroll` | Boolean | true | 是否自动滚动到顶部 |
| `hidden` | Boolean | false | 是否隐藏 |

---

## 九、分页组件 Events

| 事件名 | 参数 | 说明 |
|--------|------|------|
| `update:page` | page | 页码改变时触发 |
| `update:limit` | limit | 每页条数改变时触发 |
| `pagination` | { page, limit } | 分页参数改变时触发 |

---

## 十、分页最佳实践

### 10.1 性能优化

```javascript
// 使用 v-show 而非 v-if
<pagination v-show="total > 0" ... />

// 避免频繁请求
import { debounce } from '@/utils/index'
const getList = debounce(_getList, 300)
```

### 10.2 用户体验优化

```javascript
// 加载状态
const loading = ref(false)

function getList() {
  loading.value = true
  listUser(queryParams).then(res => {
    userList.value = res.rows
    total.value = res.total
  }).finally(() => {
    loading.value = false
  })
}
```

### 10.3 响应式适配

分页组件已内置响应式适配：

```javascript
// 移动端显示更少的页码按钮
pagerCount: {
  type: Number,
  default: document.body.clientWidth < 992 ? 5 : 7
}
```
