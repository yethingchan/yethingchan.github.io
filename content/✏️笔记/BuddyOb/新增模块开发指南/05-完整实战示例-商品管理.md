# 05 · 完整实战示例：从零加一个"商品管理"模块

> 把前面 4 篇的知识点串成一次完整实操。
> 跟着这 7 步走，就能加出一个可用的"商品管理"页面。所有代码可直接复制。

---

## 总览：本次要动的文件

```
新增（后端 4 个）：
  backend/src/main/java/com/example/admin/domain/Goods.java
  backend/src/main/java/com/example/admin/mapper/GoodsMapper.java
  backend/src/main/java/com/example/admin/service/GoodsService.java
  backend/src/main/java/com/example/admin/controller/business/GoodsController.java

新增（前端 2 个）：
  frontend/src/api/business/goods.js
  frontend/src/views/business/goods/index.vue

修改（SQL 2 个）：
  backend/src/main/resources/schema-mysql.sql   （加建表）
  backend/src/main/resources/data.sql           （加菜单/权限）
```

---

## 步骤 1：建表

在 `schema-mysql.sql` 末尾追加：

```sql
CREATE TABLE IF NOT EXISTS `biz_goods` (
  `goods_id`    BIGINT NOT NULL AUTO_INCREMENT,
  `goods_name`  VARCHAR(100) DEFAULT '',
  `goods_code`  VARCHAR(50)  DEFAULT '',
  `stock`       INT          DEFAULT 0,
  `status`      CHAR(1)      DEFAULT '0',
  `create_by`   VARCHAR(50)  DEFAULT '',
  `create_time` DATETIME,
  `remark`      VARCHAR(500) DEFAULT '',
  PRIMARY KEY (`goods_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 步骤 2：实体类

`backend/src/main/java/com/example/admin/domain/Goods.java`

```java
package com.example.admin.domain;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

import java.io.Serializable;
import java.util.Date;

@Data
@TableName("biz_goods")
public class Goods implements Serializable {
    @TableId(type = IdType.AUTO)
    private Long goodsId;
    private String goodsName;
    private String goodsCode;
    private Integer stock;
    private String status;
    private String createBy;
    private Date createTime;
    private String remark;
}
```

---

## 步骤 3：Mapper

`backend/src/main/java/com/example/admin/mapper/GoodsMapper.java`

```java
package com.example.admin.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.example.admin.domain.Goods;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface GoodsMapper extends BaseMapper<Goods> {
}
```

---

## 步骤 4：Service

`backend/src/main/java/com/example/admin/service/GoodsService.java`

```java
package com.example.admin.service;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import com.example.admin.common.utils.StringUtils;
import com.example.admin.domain.Goods;
import com.example.admin.mapper.GoodsMapper;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
@RequiredArgsConstructor
public class GoodsService {

    private final GoodsMapper goodsMapper;

    public IPage<Goods> list(IPage<Goods> page, String goodsName, String status) {
        LambdaQueryWrapper<Goods> q = new LambdaQueryWrapper<>();
        q.like(StringUtils.isNotBlank(goodsName), Goods::getGoodsName, goodsName);
        q.eq(StringUtils.isNotBlank(status), Goods::getStatus, status);
        q.orderByDesc(Goods::getGoodsId);
        return goodsMapper.selectPage(page, q);
    }

    public Goods get(Long id) {
        return goodsMapper.selectById(id);
    }

    public void add(Goods goods) {
        goodsMapper.insert(goods);
    }

    public void update(Goods goods) {
        goodsMapper.updateById(goods);
    }

    public void delete(List<Long> ids) {
        goodsMapper.deleteBatchIds(ids);
    }
}
```

---

## 步骤 5：Controller

`backend/src/main/java/com/example/admin/controller/business/GoodsController.java`

```java
package com.example.admin.controller.business;

import com.baomidou.mybatisplus.core.metadata.IPage;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.example.admin.annotation.Log;
import com.example.admin.common.AjaxResult;
import com.example.admin.domain.Goods;
import com.example.admin.service.GoodsService;
import lombok.RequiredArgsConstructor;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/business/goods")
@RequiredArgsConstructor
public class GoodsController {

    private final GoodsService goodsService;

    @PreAuthorize("@ps.hasPermi('business:goods:list')")
    @GetMapping("/list")
    public AjaxResult list(@RequestParam(defaultValue = "1") int pageNum,
                           @RequestParam(defaultValue = "10") int pageSize,
                           String goodsName, String status) {
        IPage<Goods> page = goodsService.list(new Page<>(pageNum, pageSize), goodsName, status);
        return AjaxResult.success(page.getRecords(), page.getTotal());
    }

    @PreAuthorize("@ps.hasPermi('business:goods:query')")
    @GetMapping("/{id}")
    public AjaxResult get(@PathVariable Long id) {
        return AjaxResult.success(goodsService.get(id));
    }

    @PreAuthorize("@ps.hasPermi('business:goods:add')")
    @Log(title = "商品管理", businessType = 1)
    @PostMapping
    public AjaxResult add(@RequestBody Goods goods) {
        goodsService.add(goods);
        return AjaxResult.success();
    }

    @PreAuthorize("@ps.hasPermi('business:goods:edit')")
    @Log(title = "商品管理", businessType = 2)
    @PutMapping
    public AjaxResult edit(@RequestBody Goods goods) {
        goodsService.update(goods);
        return AjaxResult.success();
    }

    @PreAuthorize("@ps.hasPermi('business:goods:remove')")
    @Log(title = "商品管理", businessType = 3)
    @DeleteMapping
    public AjaxResult remove(@RequestParam List<Long> ids) {
        goodsService.delete(ids);
        return AjaxResult.success();
    }
}
```

---

## 步骤 6：前端两个文件

### 6.1 API：`frontend/src/api/business/goods.js`

```js
import request from '@/utils/request'

export function listGoods(params) {
  return request({ url: '/business/goods/list', method: 'get', params })
}
export function getGoods(id) {
  return request({ url: '/business/goods/' + id, method: 'get' })
}
export function addGoods(data) {
  return request({ url: '/business/goods', method: 'post', data })
}
export function editGoods(data) {
  return request({ url: '/business/goods', method: 'put', data })
}
export function removeGoods(ids) {
  return request({ url: '/business/goods', method: 'delete', params: { ids } })
}
```

### 6.2 页面：`frontend/src/views/business/goods/index.vue`

> 完整代码见 [03-前端开发指南.md](./03-前端开发指南.md) 的"文件 2：页面组件"，直接复制即可（字段已是 goods）。

---

## 步骤 7：配菜单 + 权限（data.sql）

在 `data.sql` 追加（menu_id 按你库里的实际最大值往后排，这里用 31 / 50~54）：

```sql
-- 菜单（挂在业务模块目录 id=3 下）
INSERT INTO sys_menu(menu_id, parent_id, order_num, menu_name, path, component, query, menu_type, perms, icon, status, create_time) VALUES
(31, 3, 2, '商品管理', 'goods', 'business/goods/index', '', 'C', '', 'Goods', '0', '2024-01-01 00:00:00');

-- 按钮权限
INSERT INTO sys_menu(menu_id, parent_id, order_num, menu_name, path, component, query, menu_type, perms, icon, status, create_time) VALUES
(50, 31, 1, '商品查询', '', '', '', 'F', 'business:goods:list',   '#', '0', '2024-01-01 00:00:00'),
(51, 31, 2, '商品查看', '', '', '', 'F', 'business:goods:query',  '#', '0', '2024-01-01 00:00:00'),
(52, 31, 3, '商品新增', '', '', '', 'F', 'business:goods:add',    '#', '0', '2024-01-01 00:00:00'),
(53, 31, 4, '商品修改', '', '', '', 'F', 'business:goods:edit',   '#', '0', '2024-01-01 00:00:00'),
(54, 31, 5, '商品删除', '', '', '', 'F', 'business:goods:remove', '#', '0', '2024-01-01 00:00:00');

-- 角色授权
INSERT INTO sys_role_menu(role_id, menu_id) VALUES
(1, 31), (1, 50), (1, 51), (1, 52), (1, 53), (1, 54),
(2, 31), (2, 50), (2, 51), (2, 52), (2, 53);
```

在 MySQL 里执行这几条 INSERT（不要重跑整个 data.sql）：

```sql
USE ruoyi;
-- 先执行 schema-mysql.sql 里新加的 CREATE TABLE biz_goods
-- 再执行上面这几条 INSERT
```

---

## 步骤 8：验证

1. 重启后端：`cd backend && mvn spring-boot:run`
2. 前端已在跑就直接刷新，未跑则 `cd frontend && npm run dev`
3. 用 `admin / 123456` 重新登录（重新登录才会重新加载菜单和权限）
4. 侧边栏"业务模块"下应出现"商品管理"
5. 进入页面，测试：查询、新增、编辑、删除、分页

**如果菜单没出现 / 按钮没显示 / 接口 403**，对照 [06-注意事项与常见坑.md](./06-注意事项与常见坑.md) 排查。

---

## 一图回顾本次改动

```
biz_goods 表 ←── Goods.java ←── GoodsMapper ←── GoodsService ←── GoodsController
                                                                      │ /business/goods/*
                                                                      ▼
                          sys_menu(菜单C + 按钮F) + sys_role_menu(授权)
                                                                      │
                             /getRouters + /getInfo 下发                │
                                                                      ▼
        goods.js (api) ──▶ goods/index.vue (页面) ──▶ 侧边栏菜单 + 按钮权限
```

下一步 → [06-注意事项与常见坑.md](./06-注意事项与常见坑.md)
