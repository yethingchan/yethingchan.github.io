# 2026-08-10 工作日志

## MyBatisPlus 目录链接修复
- 检查了 MyBatisPlus 目录下全部 16 个 md 文件的 Obsidian 双链
- 问题：所有同目录内部链接使用了 `[[MyBatisPlus/filename]]` 绝对路径前缀，应改为最短相对路径 `[[filename]]`
- 修复：将 16 个文件中所有 `[[MyBatisPlus/` 替换为 `[[`（共约 50 处链接）
- 跨目录链接 `[[../OtherFolder/filename]]` 已验证全部正确指向真实文件，无需修改
- 文件 `04-IServer与ServiceImpl.md` 文件名含 "IServer"（非 IService），链接已与之匹配
