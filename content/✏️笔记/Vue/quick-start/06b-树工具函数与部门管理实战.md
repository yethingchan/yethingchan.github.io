# 第六章（下）：树工具函数与部门管理实战

---

## 五、树形数据操作工具函数

### 5.1 工具函数完整代码

```js
// src/utils/tree.js

/**
 * 列表转树形结构
 * 将扁平列表转为嵌套的树形结构
 *
 * @param {Array} list - 扁平列表，每项需包含 id 和 parentId
 * @param {Object} options - 配置项
 * @param {string} options.id - ID 字段名，默认 'id'
 * @param {string} options.parentId - 父 ID 字段名，默认 'parentId'
 * @param {string} options.children - 子节点字段名，默认 'children'
 * @param {number|string} options.rootId - 根节点的父 ID 值，默认 0
 * @returns {Array} 树形结构数组
 *
 * @example
 * const list = [
 *   { id: 1, parentId: 0, name: '技术部' },
 *   { id: 2, parentId: 0, name: '产品部' },
 *   { id: 11, parentId: 1, name: '前端组' },
 *   { id: 12, parentId: 1, name: '后端组' },
 * ]
 * const tree = listToTree(list, { rootId: 0 })
 * // => [{ id: 1, name: '技术部', children: [{ id: 11, name: '前端组' }, ...] }, ...]
 */
export function listToTree(list, options = {}) {
  const {
    id = 'id',
    parentId = 'parentId',
    children = 'children',
    rootId = 0
  } = options

  const map = {}      // id => 节点引用的映射表
  const tree = []     // 最终的树形结果

  // 第一次遍历：建立 id => node 的映射，并初始化 children
  list.forEach((item) => {
    map[item[id]] = { ...item, [children]: [] }
  })

  // 第二次遍历：根据 parentId 组装树形结构
  list.forEach((item) => {
    const node = map[item[id]]
    if (item[parentId] === rootId) {
      // 根节点：直接放入 tree 数组
      tree.push(node)
    } else if (map[item[parentId]]) {
      // 非根节点：挂到父节点的 children 下
      map[item[parentId]][children].push(node)
    }
  })

  return tree
}

/**
 * 树形转列表（扁平化）
 * 将嵌套树形结构转为扁平列表
 *
 * @param {Array} tree - 树形结构数组
 * @param {Object} options - 配置项
 * @param {string} options.children - 子节点字段名，默认 'children'
 * @param {Function} options.onEach - 遍历每项时的回调，可额外注入字段
 * @returns {Array} 扁平列表
 *
 * @example
 * const flat = treeToList(tree, {
 *   onEach: (item, parent, level) => {
 *     item.level = level
 *     item.parentName = parent?.name || ''
 *   }
 * })
 */
export function treeToList(tree, options = {}) {
  const { children = 'children', onEach } = options
  const list = []

  const traverse = (nodes, parent = null, level = 1) => {
    nodes.forEach((node) => {
      const item = { ...node }
      // 移除 children 引用（避免循环引用）
      // 但保留 children 的副本以便后续使用
      if (item[children]) {
        const childNodes = item[children]
        delete item[children]
        // 执行回调
        if (onEach) {
          onEach(item, parent, level)
        }
        list.push(item)
        // 递归遍历子节点
        traverse(childNodes, item, level + 1)
      } else {
        if (onEach) {
          onEach(item, parent, level)
        }
        list.push(item)
      }
    })
  }

  traverse(tree)
  return list
}

/**
 * 查找树中的某个节点（深度优先搜索）
 *
 * @param {Array} tree - 树形结构数组
 * @param {Function} predicate - 匹配函数，返回 true 表示找到
 * @param {Object} options - 配置项
 * @param {string} options.children - 子节点字段名，默认 'children'
 * @returns {Object|null} 找到的节点，未找到返回 null
 *
 * @example
 * const node = findTreeNode(tree, (node) => node.id === 112)
 */
export function findTreeNode(tree, predicate, options = {}) {
  const { children = 'children' } = options

  for (const node of tree) {
    if (predicate(node)) {
      return node
    }
    if (node[children]?.length) {
      const found = findTreeNode(node[children], predicate, options)
      if (found) return found
    }
  }

  return null
}

/**
 * 查找从根节点到目标节点的路径
 *
 * @param {Array} tree - 树形结构数组
 * @param {Function} predicate - 匹配函数，返回 true 表示目标节点
 * @param {Object} options - 配置项
 * @param {string} options.children - 子节点字段名，默认 'children'
 * @returns {Array} 路径数组（从根到目标），未找到返回空数组
 *
 * @example
 * const path = findTreePath(tree, (node) => node.id === 112)
 * // => [{ id: 1, label: '技术部' }, { id: 11, label: '前端组' }, { id: 112, label: 'Vue 小组' }]
 */
export function findTreePath(tree, predicate, options = {}) {
  const { children = 'children' } = options
  const path = []

  const dfs = (nodes) => {
    for (const node of nodes) {
      path.push(node)

      if (predicate(node)) {
        return true  // 找到目标，路径已完整
      }

      if (node[children]?.length && dfs(node[children])) {
        return true
      }

      path.pop()  // 回溯
    }

    return false
  }

  dfs(tree)
  return path
}

/**
 * 向树中添加节点
 *
 * @param {Array} tree - 树形结构数组
 * @param {number|string} parentId - 目标父节点的 ID
 * @param {Object} newNode - 要添加的新节点
 * @param {Object} options - 配置项
 * @param {string} options.id - ID 字段名，默认 'id'
 * @param {string} options.children - 子节点字段名，默认 'children'
 * @returns {boolean} 是否添加成功
 *
 * @example
 * addTreeNode(tree, 1, { id: 14, label: 'DevOps 组' })
 */
export function addTreeNode(tree, parentId, newNode, options = {}) {
  const { id = 'id', children = 'children' } = options

  const targetNode = findTreeNode(tree, (node) => node[id] === parentId, options)

  if (targetNode) {
    if (!targetNode[children]) {
      targetNode[children] = []
    }
    targetNode[children].push(newNode)
    return true
  }

  return false
}

/**
 * 从树中删除节点
 *
 * @param {Array} tree - 树形结构数组
 * @param {Function} predicate - 匹配要删除的节点
 * @param {Object} options - 配置项
 * @param {string} options.children - 子节点字段名，默认 'children'
 * @returns {Object|null} 被删除的节点，未找到返回 null
 *
 * @example
 * const removed = removeTreeNode(tree, (node) => node.id === 112)
 */
export function removeTreeNode(tree, predicate, options = {}) {
  const { children = 'children' } = options

  for (let i = 0; i < tree.length; i++) {
    if (predicate(tree[i])) {
      return tree.splice(i, 1)[0]
    }

    if (tree[i][children]?.length) {
      const removed = removeTreeNode(tree[i][children], predicate, options)
      if (removed) return removed
    }
  }

  return null
}
```

### 5.2 工具函数使用示例

```vue
<!-- src/views/tree/TreeUtilsDemo.vue -->
<template>
  <div class="tree-utils-demo">
    <h3>树形数据操作工具函数</h3>

    <el-card style="margin-bottom: 16px;">
      <template #header>
        <span>原始扁平数据（接口返回的列表格式）</span>
      </template>
      <pre class="code-block">{{ flatList }}</pre>
    </el-card>

    <el-row :gutter="16">
      <el-col :span="8">
        <el-card>
          <template #header><span>listToTree 结果</span></template>
          <pre class="code-block small">{{ treeResult }}</pre>
        </el-card>
      </el-col>
      <el-col :span="8">
        <el-card>
          <template #header><span>treeToList 结果（带层级）</span></template>
          <pre class="code-block small">{{ listResult }}</pre>
        </el-card>
      </el-col>
      <el-col :span="8">
        <el-card>
          <template #header><span>findTreePath (id=113)</span></template>
          <pre class="code-block small">{{ pathResult }}</pre>
        </el-card>
      </el-col>
    </el-row>

    <el-card style="margin-top: 16px;">
      <template #header><span>操作演示</span></template>
      <div style="display: flex; gap: 8px; margin-bottom: 12px;">
        <el-button type="primary" @click="handleAdd">添加节点</el-button>
        <el-button type="danger" @click="handleRemove">删除节点 id=112</el-button>
        <el-button @click="handleFind">查找节点 id=12</el-button>
      </div>
      <pre class="code-block">{{ currentTree }}</pre>
    </el-card>
  </div>
</template>

<script setup>
import { ref, computed } from 'vue'
import { ElMessage } from 'element-plus'
import {
  listToTree,
  treeToList,
  findTreeNode,
  findTreePath,
  addTreeNode,
  removeTreeNode
} from '@/utils/tree'

// 模拟后端接口返回的扁平列表
const flatList = ref([
  { id: 1, parentId: 0, label: '技术部', sort: 1 },
  { id: 2, parentId: 0, label: '产品部', sort: 2 },
  { id: 3, parentId: 0, label: '运营部', sort: 3 },
  { id: 11, parentId: 1, label: '前端组', sort: 1 },
  { id: 12, parentId: 1, label: '后端组', sort: 2 },
  { id: 13, parentId: 1, label: '测试组', sort: 3 },
  { id: 111, parentId: 11, label: 'Vue 小组', sort: 1 },
  { id: 112, parentId: 11, label: 'React 小组', sort: 2 },
  { id: 113, parentId: 11, label: '小程序组', sort: 3 },
  { id: 21, parentId: 2, label: '产品设计组', sort: 1 },
  { id: 22, parentId: 2, label: '用户研究组', sort: 2 },
  { id: 31, parentId: 3, label: '内容运营', sort: 1 },
  { id: 32, parentId: 3, label: '活动运营', sort: 2 }
])

// 列表转树
const treeResult = computed(() => {
  return JSON.stringify(listToTree(flatList.value, { rootId: 0 }), null, 2)
})

// 当前操作用的树（可变）
const currentTree = computed(() => {
  return JSON.stringify(
    listToTree(flatList.value, { rootId: 0 }),
    null,
    2
  )
})

// 树转列表（带层级信息）
const listResult = computed(() => {
  const tree = listToTree(flatList.value, { rootId: 0 })
  const flat = treeToList(tree, {
    onEach: (item, parent, level) => {
      item._level = level
      item._parentName = parent?.label || '(根)'
    }
  })
  return flat.map((item) => `${'  '.repeat(item._level - 1)}${item._level}.${item.label} (父: ${item._parentName})`).join('\n')
})

// 查找路径
const pathResult = computed(() => {
  const tree = listToTree(flatList.value, { rootId: 0 })
  const path = findTreePath(tree, (node) => node.id === 113)
  return path.map((node) => node.label).join(' > ')
})

// 添加节点
const handleAdd = () => {
  addTreeNode(
    listToTree(flatList.value, { rootId: 0 }),
    2,
    { id: 23, label: '增长组', sort: 3 }
  )
  // 注意：因为 listToTree 返回新对象，所以直接操作 flatList
  flatList.value.push({ id: 23, parentId: 2, label: '增长组', sort: 3 })
  ElMessage.success('已添加节点「增长组」到「产品部」')
}

// 删除节点
const handleRemove = () => {
  const index = flatList.value.findIndex((item) => item.id === 112)
  if (index !== -1) {
    const removed = flatList.value.splice(index, 1)
    ElMessage.success(`已删除：${removed[0].label}`)
  }
}

// 查找节点
const handleFind = () => {
  const tree = listToTree(flatList.value, { rootId: 0 })
  const node = findTreeNode(tree, (n) => n.id === 12)
  if (node) {
    ElMessage.success(`找到节点：${JSON.stringify(node)}`)
  }
}
</script>

<style scoped>
.tree-utils-demo {
  padding: 20px;
}

.code-block {
  background: #f5f7fa;
  padding: 12px;
  border-radius: 4px;
  font-size: 12px;
  overflow-x: auto;
  max-height: 400px;
  overflow-y: auto;
}

.code-block.small {
  max-height: 300px;
  font-size: 11px;
}
</style>
```

---

## 六、综合示例：部门管理页面

### 6.1 需求分析

实现一个完整的部门管理页面，包含以下功能：

```
┌───────────────────────────────────────────────────────────┐
│ 部门管理                                                   │
├─────────────────┬─────────────────────────────────────────┤
│                 │  部门详情 / 员工列表                       │
│  部门树         │                                         │
│  ┌───────────┐  │  名称：技术中心                           │
│  │ 总公司     │  │  负责人：张三                            │
│  │ ├ 技术中心 │  │  人数：45                               │
│  │ │ ├ 前端   │  │                                         │
│  │ │ └ 后端   │  │  员工列表：                             │
│  │ ├ 产品中心 │  │  ┌────┬────┬────┐                      │
│  │ └ 运营中心 │  │  │ 姓名│职位│操作│                      │
│  └───────────┘  │  │ 张三│组长│... │                      │
│                 │  └────┴────┴────┘                      │
│  [新增] [展开]   │                                         │
├─────────────────┴─────────────────────────────────────────┤
│ 支持功能：右键菜单、拖拽排序、增删改查                      │
└───────────────────────────────────────────────────────────┘
```

### 6.2 完整代码

```vue
<!-- src/views/system/dept/index.vue -->
<template>
  <div class="dept-manage">
    <!-- 页面标题 -->
    <div class="page-header">
      <h2>部门管理</h2>
      <p class="subtitle">管理部门组织架构，支持新增、编辑、删除及拖拽排序</p>
    </div>

    <div class="dept-layout">
      <!-- ========== 左侧：部门树 ========== -->
      <el-card class="dept-tree-card">
        <template #header>
          <div class="card-header">
            <span>组织架构</span>
            <div>
              <el-button size="small" @click="toggleExpandAll">
                {{ isExpandAll ? '折叠全部' : '展开全部' }}
              </el-button>
              <el-button type="primary" size="small" @click="handleAddDept(null)">
                <el-icon><Plus /></el-icon> 新增部门
              </el-button>
            </div>
          </div>
        </template>

        <el-input
          v-model="filterText"
          placeholder="搜索部门名称"
          clearable
          size="small"
          style="margin-bottom: 8px;"
        />

        <!-- 部门树：支持拖拽排序 -->
        <el-tree
          ref="treeRef"
          :data="deptTree"
          node-key="id"
          :filter-node-method="filterNode"
          :expand-on-click-node="false"
          highlight-current
          :default-expanded-keys="defaultExpanded"
          :draggable="true"
          :allow-drop="allowDrop"
          @node-click="handleNodeClick"
          @node-contextmenu="handleContextMenu"
          @node-drop="handleDrop"
          @node-drag-end="handleDragEnd"
        >
          <template #default="{ node, data }">
            <div
              class="dept-node"
              :class="{ 'is-active': currentDept?.id === data.id }"
              @click="handleNodeClick(data)"
            >
              <!-- 部门图标 -->
              <el-icon class="dept-icon">
                <OfficeBuilding v-if="data.children?.length" />
                <Flag v-else />
              </el-icon>

              <!-- 部门名称 -->
              <span class="dept-name">{{ data.label }}</span>

              <!-- 人数 -->
              <span class="dept-count">({{ data.count || 0 }})</span>

              <!-- 操作按钮 -->
              <span class="dept-actions" @click.stop>
                <el-button
                  type="primary"
                  link
                  size="small"
                  title="新增子部门"
                  @click.stop="handleAddDept(data)"
                >
                  <el-icon><Plus /></el-icon>
                </el-button>
                <el-button
                  type="primary"
                  link
                  size="small"
                  title="编辑"
                  @click.stop="handleEditDept(data)"
                >
                  <el-icon><Edit /></el-icon>
                </el-button>
                <el-popconfirm
                  title="确定删除该部门及其子部门吗？"
                  @confirm="handleDeleteDept(data)"
                >
                  <template #reference>
                    <el-button type="danger" link size="small" title="删除">
                      <el-icon><Delete /></el-icon>
                    </el-button>
                  </template>
                </el-popconfirm>
              </span>
            </div>
          </template>
        </el-tree>
      </el-card>

      <!-- ========== 右侧：部门详情与员工列表 ========== -->
      <el-card class="dept-detail-card">
        <template v-if="currentDept">
          <template #header>
            <div class="card-header">
              <span>{{ currentDept.label }} - 部门详情</span>
              <el-button type="primary" size="small" @click="handleAddEmployee">
                <el-icon><Plus /></el-icon> 添加员工
              </el-button>
            </div>
          </template>

          <!-- 部门基本信息 -->
          <el-descriptions :column="3" border style="margin-bottom: 24px;">
            <el-descriptions-item label="部门名称">{{ currentDept.label }}</el-descriptions-item>
            <el-descriptions-item label="负责人">{{ currentDept.leader || '-' }}</el-descriptions-item>
            <el-descriptions-item label="人数">{{ currentDept.count || 0 }}</el-descriptions-item>
            <el-descriptions-item label="排序">{{ currentDept.sort }}</el-descriptions-item>
            <el-descriptions-item label="状态">
              <el-tag :type="currentDept.status === 1 ? 'success' : 'danger'" size="small">
                {{ currentDept.status === 1 ? '正常' : '停用' }}
              </el-tag>
            </el-descriptions-item>
            <el-descriptions-item label="创建时间">{{ currentDept.createTime }}</el-descriptions-item>
          </el-descriptions>

          <!-- 员工列表 -->
          <h4 style="margin-bottom: 12px;">员工列表</h4>
          <el-table :data="employeeList" border stripe size="small">
            <el-table-column prop="name" label="姓名" width="120" />
            <el-table-column prop="position" label="职位" width="150" />
            <el-table-column prop="phone" label="手机号" width="140" />
            <el-table-column prop="email" label="邮箱" min-width="200" />
            <el-table-column prop="joinDate" label="入职日期" width="120" />
            <el-table-column label="操作" width="140" align="center">
              <template #default="{ row }">
                <el-button type="primary" link size="small" @click="handleEditEmployee(row)">
                  编辑
                </el-button>
                <el-popconfirm title="确定移除该员工？" @confirm="handleRemoveEmployee(row)">
                  <template #reference>
                    <el-button type="danger" link size="small">移除</el-button>
                  </template>
                </el-popconfirm>
              </template>
            </el-table-column>
          </el-table>
        </template>

        <el-empty v-else description="请在左侧选择一个部门" />
      </el-card>
    </div>

    <!-- ========== 右键菜单 ========== -->
    <div
      v-show="contextMenuVisible"
      class="context-menu"
      :style="{ left: contextMenuLeft + 'px', top: contextMenuTop + 'px' }"
    >
      <div class="context-menu-item" @click="handleAddDept(contextMenuNode)">
        <el-icon><Plus /></el-icon> 新增子部门
      </div>
      <div class="context-menu-item" @click="handleEditDept(contextMenuNode)">
        <el-icon><Edit /></el-icon> 编辑部门
      </div>
      <div class="context-menu-divider" />
      <div class="context-menu-item danger" @click="handleDeleteDept(contextMenuNode)">
        <el-icon><Delete /></el-icon> 删除部门
      </div>
      <div class="context-menu-divider" />
      <div class="context-menu-item" @click="handleExpandChildren(contextMenuNode)">
        <el-icon><ArrowDown /></el-icon> 展开子节点
      </div>
      <div class="context-menu-item" @click="handleCollapseChildren(contextMenuNode)">
        <el-icon><ArrowUp /></el-icon> 折叠子节点
      </div>
    </div>

    <!-- ========== 新增/编辑部门弹窗 ========== -->
    <el-dialog
      v-model="deptDialogVisible"
      :title="deptDialogTitle"
      width="500px"
      destroy-on-close
    >
      <el-form
        ref="deptFormRef"
        :model="deptForm"
        :rules="deptFormRules"
        label-width="80px"
      >
        <el-form-item label="上级部门">
          <el-input :value="deptForm.parentName" disabled />
        </el-form-item>
        <el-form-item label="部门名称" prop="label">
          <el-input v-model="deptForm.label" placeholder="请输入部门名称" />
        </el-form-item>
        <el-form-item label="负责人" prop="leader">
          <el-input v-model="deptForm.leader" placeholder="请输入负责人" />
        </el-form-item>
        <el-form-item label="排序" prop="sort">
          <el-input-number v-model="deptForm.sort" :min="0" :max="999" />
        </el-form-item>
        <el-form-item label="状态">
          <el-radio-group v-model="deptForm.status">
            <el-radio :value="1">正常</el-radio>
            <el-radio :value="0">停用</el-radio>
          </el-radio-group>
        </el-form-item>
      </el-form>

      <template #footer>
        <el-button @click="deptDialogVisible = false">取消</el-button>
        <el-button type="primary" @click="handleSubmitDept">确定</el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup>
import { ref, watch, nextTick, onMounted, onBeforeUnmount } from 'vue'
import { ElMessage } from 'element-plus'
import {
  Plus,
  Edit,
  Delete,
  ArrowDown,
  ArrowUp,
  OfficeBuilding,
  Flag
} from '@element-plus/icons-vue'
import {
  listToTree,
  treeToList,
  findTreeNode,
  findTreePath,
  addTreeNode,
  removeTreeNode
} from '@/utils/tree'

// ==================== 数据 ====================

// 部门扁平数据（模拟后端接口）
const deptFlatList = ref([
  { id: 1, parentId: 0, label: '总公司', leader: '王总', sort: 0, status: 1, count: 120, createTime: '2024-01-01' },
  { id: 11, parentId: 1, label: '技术中心', leader: '张总', sort: 1, status: 1, count: 45, createTime: '2024-01-01' },
  { id: 111, parentId: 11, label: '前端开发部', leader: '李前端', sort: 1, status: 1, count: 15, createTime: '2024-03-01' },
  { id: 112, parentId: 11, label: '后端开发部', leader: '赵后端', sort: 2, status: 1, count: 18, createTime: '2024-03-01' },
  { id: 113, parentId: 11, label: '测试部', leader: '钱测试', sort: 3, status: 1, count: 12, createTime: '2024-03-01' },
  { id: 12, parentId: 1, label: '产品中心', leader: '刘产品', sort: 2, status: 1, count: 25, createTime: '2024-01-01' },
  { id: 121, parentId: 12, label: '产品设计部', leader: '陈设计', sort: 1, status: 1, count: 10, createTime: '2024-06-01' },
  { id: 122, parentId: 12, label: '用户研究部', leader: '吴研究', sort: 2, status: 1, count: 8, createTime: '2024-06-01' },
  { id: 123, parentId: 12, label: '数据分析部', leader: '周数据', sort: 3, status: 1, count: 7, createTime: '2024-06-01' },
  { id: 13, parentId: 1, label: '运营中心', leader: '孙运营', sort: 3, status: 1, count: 30, createTime: '2024-01-01' },
  { id: 131, parentId: 13, label: '内容运营部', leader: '郑内容', sort: 1, status: 1, count: 12, createTime: '2024-09-01' },
  { id: 132, parentId: 13, label: '市场推广部', leader: '王市场', sort: 2, status: 1, count: 18, createTime: '2024-09-01' },
  { id: 14, parentId: 1, label: '行政人事部', leader: '冯行政', sort: 4, status: 1, count: 20, createTime: '2024-01-01' }
])

// 将扁平数据转为树形结构
const deptTree = ref([])

const refreshTree = () => {
  deptTree.value = listToTree(deptFlatList.value, { rootId: 0 })
}

// 当前选中的部门
const currentDept = ref(null)

// 员工列表（模拟数据）
const employeeMap = {
  111: [
    { id: 1, name: '张三', position: '前端组长', phone: '13800001111', email: 'zhangsan@company.com', joinDate: '2024-03-15' },
    { id: 2, name: '李四', position: '高级前端工程师', phone: '13800002222', email: 'lisi@company.com', joinDate: '2024-05-20' },
    { id: 3, name: '王五', position: '前端工程师', phone: '13800003333', email: 'wangwu@company.com', joinDate: '2024-08-01' }
  ],
  112: [
    { id: 4, name: '赵六', position: '后端组长', phone: '13800004444', email: 'zhaoliu@company.com', joinDate: '2024-02-10' },
    { id: 5, name: '孙七', position: 'Java 工程师', phone: '13800005555', email: 'sunqi@company.com', joinDate: '2024-04-15' }
  ],
  121: [
    { id: 6, name: '周八', position: 'UI 设计师', phone: '13800006666', email: 'zhouba@company.com', joinDate: '2024-06-20' },
    { id: 7, name: '吴九', position: '交互设计师', phone: '13800007777', email: 'wujiu@company.com', joinDate: '2024-07-01' }
  ],
  14: [
    { id: 8, name: '郑十', position: 'HR 经理', phone: '13800008888', email: 'zhengshi@company.com', joinDate: '2024-01-15' },
    { id: 9, name: '钱十一', position: '招聘专员', phone: '13800009999', email: 'qian11@company.com', joinDate: '2024-03-01' }
  ]
}

const employeeList = ref([])

// ==================== 树操作 ====================

const treeRef = ref(null)
const filterText = ref('')
const isExpandAll = ref(true)
const defaultExpanded = ref([1])

// 过滤
const filterNode = (value, data) => {
  if (!value) return true
  return data.label.includes(value)
}

watch(filterText, (val) => {
  treeRef.value?.filter(val)
})

// 展开/折叠全部
const toggleExpandAll = () => {
  isExpandAll.value = !isExpandAll.value
  const nodes = treeRef.value?.store?.root?.childNodes || []
  const toggle = (node) => {
    node.expanded = isExpandAll.value
    if (node.childNodes) {
      node.childNodes.forEach((child) => toggle(child))
    }
  }
  nodes.forEach((node) => toggle(node))
}

// 点击节点
const handleNodeClick = (data) => {
  currentDept.value = data
  employeeList.value = employeeMap[data.id] || []
}

// ==================== 右键菜单 ====================

const contextMenuVisible = ref(false)
const contextMenuLeft = ref(0)
const contextMenuTop = ref(0)
const contextMenuNode = ref(null)

const handleContextMenu = (event, data, node, element) => {
  event.preventDefault()
  contextMenuNode.value = data
  contextMenuLeft.value = event.clientX
  contextMenuTop.value = event.clientY
  contextMenuVisible.value = true
}

// 点击其他区域关闭右键菜单
const closeContextMenu = () => {
  contextMenuVisible.value = false
}

onMounted(() => {
  refreshTree()
  document.addEventListener('click', closeContextMenu)
})

onBeforeUnmount(() => {
  document.removeEventListener('click', closeContextMenu)
})

// 展开/折叠子节点
const handleExpandChildren = (nodeData) => {
  const node = treeRef.value?.store?.nodesMap[nodeData.id]
  if (node) {
    setExpandRecursive(node, true)
  }
  contextMenuVisible.value = false
}

const handleCollapseChildren = (nodeData) => {
  const node = treeRef.value?.store?.nodesMap[nodeData.id]
  if (node) {
    setExpandRecursive(node, false)
  }
  contextMenuVisible.value = false
}

const setExpandRecursive = (node, expanded) => {
  node.expanded = expanded
  if (node.childNodes) {
    node.childNodes.forEach((child) => setExpandRecursive(child, expanded))
  }
}

// ==================== 拖拽排序 ====================

/**
 * 拖拽放置规则
 * 只允许同级拖拽（放在节点前后），不允许拖入子节点内部
 * 这样可以保持树形结构的 parentId 不变
 */
const allowDrop = (draggingNode, dropNode, type) => {
  // type: 'prev' | 'inner' | 'next'
  // 只允许同级前后拖拽，不允许放入节点内部
  return type !== 'inner'
}

const handleDrop = (draggingNode, dropNode, dropType, ev) => {
  // 拖拽完成后，同步更新 deptFlatList 的 sort 排序
  // 因为只允许同级拖拽，parentId 不会改变，只需更新同级排序
  syncSortFromTree()
  ElMessage.success('排序已更新')
}

const handleDragEnd = () => {
  // 拖拽结束的清理工作
}

/**
 * 从树形结构同步排序到扁平列表
 */
const syncSortFromTree = () => {
  const syncLevel = (nodes) => {
    nodes.forEach((node, index) => {
      const flatItem = deptFlatList.value.find((item) => item.id === node.data.id)
      if (flatItem) {
        flatItem.sort = index + 1
      }
      if (node.childNodes?.length) {
        syncLevel(node.childNodes)
      }
    })
  }
  const rootNodes = treeRef.value?.store?.root?.childNodes || []
  syncLevel(rootNodes)
}

// ==================== 部门增删改 ====================

const deptDialogVisible = ref(false)
const deptDialogTitle = ref('新增部门')
const deptFormRef = ref(null)
const editingDeptId = ref(null)

const deptForm = ref({
  parentId: 0,
  parentName: '总公司',
  label: '',
  leader: '',
  sort: 0,
  status: 1
})

const deptFormRules = {
  label: [{ required: true, message: '请输入部门名称', trigger: 'blur' }],
  leader: [{ required: true, message: '请输入负责人', trigger: 'blur' }]
}

// 新增部门
const handleAddDept = (parentData) => {
  editingDeptId.value = null
  deptDialogTitle.value = '新增部门'

  if (parentData) {
    // 在某个节点下新增子部门
    deptForm.value = {
      parentId: parentData.id,
      parentName: parentData.label,
      label: '',
      leader: '',
      sort: 0,
      status: 1
    }
  } else {
    // 新增根级部门
    deptForm.value = {
      parentId: 0,
      parentName: '总公司',
      label: '',
      leader: '',
      sort: 0,
      status: 1
    }
  }

  deptDialogVisible.value = true
  contextMenuVisible.value = false
}

// 编辑部门
const handleEditDept = (data) => {
  editingDeptId.value = data.id
  deptDialogTitle.value = '编辑部门'

  // 查找父节点名称
  const tree = deptTree.value
  const path = findTreePath(tree, (node) => node.id === data.id)
  const parentName = path.length > 1 ? path[path.length - 2].label : '总公司'

  deptForm.value = {
    parentId: data.parentId || 0,
    parentName,
    label: data.label,
    leader: data.leader,
    sort: data.sort,
    status: data.status
  }

  deptDialogVisible.value = true
  contextMenuVisible.value = false
}

// 删除部门（级联删除子部门）
const handleDeleteDept = (data) => {
  // 找到该节点及其所有子节点的 ID
  const tree = deptTree.value
  const subtree = findTreeNode(tree, (node) => node.id === data.id)
  if (!subtree) return

  const allIds = []
  const collectIds = (nodes) => {
    nodes.forEach((node) => {
      allIds.push(node.id)
      if (node.children?.length) {
        collectIds(node.children)
      }
    })
  }
  collectIds([subtree])

  // 从扁平列表中移除这些 ID
  deptFlatList.value = deptFlatList.value.filter((item) => !allIds.includes(item.id))

  // 如果删除的是当前选中的部门，清空详情
  if (currentDept.value && allIds.includes(currentDept.value.id)) {
    currentDept.value = null
    employeeList.value = []
  }

  // 刷新树
  refreshTree()
  ElMessage.success(`已删除「${data.label}」及其子部门（共 ${allIds.length} 个）`)
  contextMenuVisible.value = false
}

// 提交部门表单
const handleSubmitDept = async () => {
  if (!deptFormRef.value) return

  await deptFormRef.value.validate((valid) => {
    if (!valid) return

    if (editingDeptId.value) {
      // 编辑模式：更新扁平列表中的对应项
      const item = deptFlatList.value.find((d) => d.id === editingDeptId.value)
      if (item) {
        Object.assign(item, {
          label: deptForm.value.label,
          leader: deptForm.value.leader,
          sort: deptForm.value.sort,
          status: deptForm.value.status
        })
      }
      // 如果编辑的是当前选中部门，更新详情
      if (currentDept.value?.id === editingDeptId.value) {
        currentDept.value = { ...currentDept.value, ...deptForm.value }
      }
      ElMessage.success('部门已更新')
    } else {
      // 新增模式：往扁平列表添加
      const newId = Date.now()
      deptFlatList.value.push({
        id: newId,
        parentId: deptForm.value.parentId,
        label: deptForm.value.label,
        leader: deptForm.value.leader,
        sort: deptForm.value.sort,
        status: deptForm.value.status,
        count: 0,
        createTime: new Date().toISOString().slice(0, 10)
      })
      ElMessage.success('部门已添加')
    }

    refreshTree()
    deptDialogVisible.value = false
  })
}

// ==================== 员工操作 ====================

const handleAddEmployee = () => {
  ElMessage.info('添加员工（弹窗表单略）')
}

const handleEditEmployee = (row) => {
  ElMessage.info(`编辑员工：${row.name}（弹窗表单略）`)
}

const handleRemoveEmployee = (row) => {
  employeeList.value = employeeList.value.filter((e) => e.id !== row.id)
  ElMessage.success(`已移除员工：${row.name}`)
}
</script>

<style scoped>
.dept-manage {
  padding: 20px;
}

.page-header h2 {
  margin: 0 0 4px;
}

.page-header .subtitle {
  color: #909399;
  font-size: 13px;
  margin: 0 0 20px;
}

.dept-layout {
  display: flex;
  gap: 16px;
  min-height: 600px;
}

.dept-tree-card {
  width: 380px;
  flex-shrink: 0;
}

.dept-detail-card {
  flex: 1;
}

.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* 部门节点样式 */
.dept-node {
  display: flex;
  align-items: center;
  gap: 6px;
  flex: 1;
  padding: 2px 4px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 14px;
  transition: background-color 0.2s;
}

.dept-node:hover {
  background-color: #f5f7fa;
}

.dept-node.is-active {
  background-color: #ecf5ff;
  color: #409eff;
}

.dept-icon {
  font-size: 16px;
  color: #909399;
  flex-shrink: 0;
}

.dept-name {
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.dept-count {
  color: #c0c4cc;
  font-size: 12px;
  flex-shrink: 0;
}

/* 操作按钮 hover 显示 */
.dept-actions {
  display: none;
  margin-left: auto;
  flex-shrink: 0;
}

.dept-node:hover .dept-actions {
  display: flex;
  align-items: center;
}

/* 右键菜单样式 */
.context-menu {
  position: fixed;
  z-index: 3000;
  background: #fff;
  border: 1px solid #e4e7ed;
  border-radius: 4px;
  padding: 4px 0;
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);
  min-width: 160px;
}

.context-menu-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 16px;
  cursor: pointer;
  font-size: 13px;
  color: #606266;
  transition: all 0.2s;
}

.context-menu-item:hover {
  background-color: #ecf5ff;
  color: #409eff;
}

.context-menu-item.danger:hover {
  background-color: #fef0f0;
  color: #f56c6c;
}

.context-menu-divider {
  height: 1px;
  background-color: #e4e7ed;
  margin: 4px 0;
}
</style>
```

### 6.3 功能清单总结

| 功能 | 实现方式 |
|------|----------|
| 树形展示 | `el-tree` + `listToTree` 工具函数将扁平数据转为树形 |
| 搜索过滤 | `filter-node-method` + `watch` 监听搜索文本 |
| 展开/折叠全部 | 遍历 `tree.store.root.childNodes` 设置 `expanded` |
| 自定义节点 | `#default="{ node, data }"` 作用域插槽 |
| 右键菜单 | `@node-contextmenu` + 固定定位的菜单 div |
| 拖拽排序 | `:draggable="true"` + `allow-drop` 限制同级拖拽 + `@node-drop` 回调 |
| 新增部门 | `el-dialog` 表单弹窗 + 往扁平列表 push 新项 + `refreshTree` |
| 编辑部门 | 弹窗回填数据 + 修改扁平列表对应项 + `refreshTree` |
| 删除部门 | 递归收集子节点 ID + 从扁平列表过滤 + `refreshTree` |
| 部门详情 | `el-descriptions` 展示基本信息 |
| 员工列表 | `el-table` 根据 `employeeMap[deptId]` 展示 |

---

## 本章小结

本章围绕后台管理系统中常见的 **树形与多级列表** 需求，系统讲解了以下内容：

1. **el-tree 基础用法**：data、node-key、默认展开、高亮选中、复选框等核心属性。
2. **自定义节点内容**：通过作用域插槽 `#default` 实现图标、数量标记、操作按钮等自定义渲染。
3. **树形表格**：利用 `el-table` 的 `row-key` + `tree-props` 实现多级折叠列表，配合展开/折叠全部按钮。
4. **懒加载**：`lazy` + `load` 属性实现异步加载子节点，避免一次性加载大量数据。
5. **工具函数**：`listToTree`、`treeToList`、`findTreeNode`、`findTreePath`、`addTreeNode`、`removeTreeNode` 六个核心工具函数，覆盖树形数据操作的全部常见场景。
6. **综合实战**：部门管理页面，左侧树 + 右侧详情/表格，支持右键菜单、拖拽排序、增删改查。

---

> **上一篇**：[第6章（上）el-tree 与树形表格](./06a-el-tree与树形表格.md)
>
> **下一章**：[第7章 登录认证与权限控制](./07-登录认证与权限.md)
>
> **章节总览**：[第6章 树形与多级列表](./06-树形与多级列表.md)
